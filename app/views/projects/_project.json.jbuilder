json.extract! project, :id, :summaryOfChallenge, :summaryOfSolution, :summaryOfOutcome, :challenge, :solution, :outcome, :comment, :order, :category, :created_at, :updated_at
json.url project_url(project, format: :json)
